using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Split : MonoBehaviour
{

    //public GameObject Player;

    //void Update()
    //{
    //    if (Input.GetKeyDown(KeyCode.W))
    //    {
    //        Eject();
    //    }
    //}

    //void Eject()
    //{
    //    Instantiate(, other.transform.position, MassBlobRotation.rotation);
    //}
    //public Transform FirePoint;
    ////public GameObject MassBlob;
    //public GameObject Player;
    //public float Speed;
    //public Rigidbody2D rb;

    //void Update()
    //{

    //    if (Input.GetButtonDown("q"))
    //    //every time the shoot button is pressed, the shoot function will be called
    //    {
    //        Split();
    //        transform.localScale -= new Vector3(Decrease, Decrease, Decrease);
    //    }
    //}

    //void Split() //executes when shoot button is called
    //{
    //    //this is the mass ejector function
    //    Instantiate(Player/* try MassBlob*/, FirePoint.position/*where to spawn*/, FirePoint.rotation);
    //}

    //void Update()
    //{
    //    rb.velocity = transform.position * Speed;
    //}
    //transform.localScale += new Vector3(Enemy1.transform.localScale.x, Enemy1.transform.localScale.x, Enemy1.transform.localScale.x);

}